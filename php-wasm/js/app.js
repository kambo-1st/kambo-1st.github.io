// PHP-WASM Demo Application
document.addEventListener('DOMContentLoaded', function() {
    console.log('PHP-WASM Demo Application loaded');
    
    // PHP-WASM is loaded via the script tag with php-tags.mjs
    // The PHP code in script type="text/php" tags is executed automatically
    // when the WASM environment is ready

    // In a more complex app, we might want to add JavaScript here to
    // handle additional functionality or events not managed by PHP
});